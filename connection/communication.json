{
    "url": "{{ifempty(parameters.baseUrl, 'https://api.opnform.com')}}/open/workspaces",
    "headers": {
        "Authorization": "Bearer {{parameters.apiKey}}"
    },
    "response": {
        "metadata": {
            "value": "{{body.length}} workspace(s)"
        },
        "error": {
            "message": "{{body.message}}"
        }
    },
    "log": {
        "sanitize": [
            "request.headers.Authorization"
        ]
    }
}
