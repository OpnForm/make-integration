{
    "url": "/open/workspaces/{{parameters.workspaceId}}/forms",
    "method": "GET",
    "response": {
        "output": {
            "label": "{{item.title}}",
            "value": "{{item.id}}"
        },
        "iterate": "{{body.data}}"
    },
    "pagination": {
        "mergeWithParent": true,
        "url": "{{body.links.next}}",
        "condition": "{{body.links.next}}"
    }
}
